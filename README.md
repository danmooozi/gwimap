# gwimap

Supabase 기반 심령 스팟 지도 MVP.

## Run

```bash
cp .env.example .env
npm install
npm run dev
```

기본 URL: `http://localhost:3000`

## API

- `GET /api/spots`
- `GET /api/spots/:id`
- `POST /api/reports`
- `POST /api/reports/:id/flags`
- `POST /api/uploads/image`
